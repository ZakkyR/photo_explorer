<Window x:Class="PhotoExplorer.App.Views.BulkTagEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="一括タグ編集" Width="360" Height="340"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title with count -->
        <TextBlock Grid.Row="0" x:Name="TitleBlock" FontWeight="Bold" Margin="0,0,0,4"/>

        <!-- Common tags label -->
        <TextBlock Grid.Row="1" Text="共通タグ:" Margin="0,0,0,4"/>

        <!-- Common tags list -->
        <ListBox Grid.Row="2" ItemsSource="{Binding CommonTags}" x:Name="CommonTagList" Margin="0,0,0,8">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button Content="×" Tag="{Binding}" Click="RemoveCommonTag_Click"
                                Padding="4,1" Background="Transparent" BorderThickness="0"
                                Foreground="Red" Cursor="Hand"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Add new tag row -->
        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewTagBox" Grid.Column="0" Margin="0,0,4,0"
                     KeyDown="NewTagBox_KeyDown"/>
            <Button Grid.Column="1" Content="追加" Click="AddTag_Click" Padding="8,2"/>
        </Grid>

        <!-- Close button -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="完了" IsDefault="True" Click="Done_Click" Padding="12,4"/>
        </StackPanel>
    </Grid>
</Window>
